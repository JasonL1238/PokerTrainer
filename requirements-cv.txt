--extra-index-url https://download.pytorch.org/whl/cpu

# Versions verified in the local poker-cv environment. The Docker build must
# pass on both amd64 and arm64 before a host architecture is accepted.
torch==2.8.0+cpu; platform_machine == "x86_64"
torchvision==0.23.0+cpu; platform_machine == "x86_64"
torch==2.8.0+cpu; platform_machine == "aarch64"
# The official aarch64 CPU wheel omits the local "+cpu" version suffix.
torchvision==0.23.0; platform_machine == "aarch64"
av==15.1.0
numpy==2.2.6
matplotlib==3.10.6
pillow==11.3.0
pyyaml==6.0.3
requests==2.32.4
scipy==1.16.2
psutil==7.1.0
polars==1.33.1
ultralytics-thop==2.0.17
