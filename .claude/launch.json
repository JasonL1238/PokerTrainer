{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "pokertrainer",
      "//": "Launched through an interactive login shell so the developer's own profile is sourced. Optional runtime settings (TEXAS_SOLVER_PATH, TEXAS_SOLVER_RESOURCE_DIR, ANTHROPIC_API_KEY) are per-machine environment variables and must not be hardcoded here.",
      "runtimeExecutable": "zsh",
      "runtimeArgs": [
        "-lic",
        "exec streamlit run app.py --server.port ${PORT:-8501} --server.headless true --browser.gatherUsageStats false"
      ],
      "port": 8501,
      "autoPort": true
    }
  ]
}
